#!/usr/bin/expect -f

set chain      [lindex $argv 0]
set passphrase [lindex $argv 1]

spawn geth --$chain account new
expect "Passphrase: "
send "$passphrase\r"
expect "Repeat passphrase: "
send "$passphrase\r"
expect eof
